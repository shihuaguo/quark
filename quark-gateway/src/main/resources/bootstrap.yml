eureka:
  instance:
    prefer-ip-address: true
  client:
    serviceUrl:
      defaultZone: http://maomao:mao123456@localhost:8761/eureka/

spring:
  application:
    name: quark-gateway

server:
  port: 8001
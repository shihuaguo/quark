spring:
  application:
    name: openstore-demo
  profiles:
    active: dev
  cloud:
    config:
      label: master
      profile: dev
      #uri: http://localhost:8777

      fail-fast: true
      discovery:
        service-id: openstore-config-server
        enabled: true